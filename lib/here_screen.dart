import 'package:flutter/material.dart';

class HereScreen extends StatelessWidget {
  const HereScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Here Screen Ko Yout par P"),
      ),
    );
  }
}
