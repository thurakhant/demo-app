import 'package:flutter/material.dart';

class LoginSuccessfulScreen extends StatelessWidget {
  const LoginSuccessfulScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text("Min Mhan dal")),
    );
  }
}
